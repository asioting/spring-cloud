spring:
  application:
    name: config-server
  cloud:
    config:
      server:
        git:
          #uri: /d/work/depot/depot-spring-cloud/config
          uri: https://github.com/asioting/spring-cloud.git
          search-paths: config
          skip-ssl-validation: true
      #uri: http://localhost:8888
